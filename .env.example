ENV_STATE=DEV
# Ollama model used by the CLI (e.g. llama3.2, mistral)
DEV_OLLAMA_MODEL=llama3.2
# Ollama API base URL (preflight, `selene model` --host default)
DEV_OLLAMA_HOST=http://localhost:11434
# Tavily API key for web search (get one at https://tavily.com)
DEV_TAVILY_API_KEY=